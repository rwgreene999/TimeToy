<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Spring theme: fresh pastels, mint greens and soft floral accents -->
    <!-- Core window colors -->
    <Color x:Key="WindowBackgroundColor">#FFF6FFF3</Color>    <!-- very pale mint -->
    <Color x:Key="WindowForegroundColor">#FF0F3A2C</Color>    <!-- deep green for readable text -->

    <!-- Primary / accents -->
    <Color x:Key="PrimaryColor">#FF49C07A</Color>             <!-- fresh spring green -->
    <Color x:Key="PrimaryLightColor">#FFA6F4CC</Color>        <!-- pale mint highlight -->
    <Color x:Key="PrimaryDarkColor">#FF19743F</Color>         <!-- dark green -->
    <Color x:Key="AccentColor">#FFFF9AB8</Color>              <!-- soft floral pink -->
    <Color x:Key="AccentDarkColor">#FFB24B6B</Color>

    <!-- Surface / neutrals -->
    <Color x:Key="BackgroundColor">#FFF6FFF3</Color>
    <Color x:Key="SurfaceColor">#FFF0F8EE</Color>
    <Color x:Key="ElevatedSurfaceColor">#FFE6F3E8</Color>

    <!-- Text palettes -->
    <Color x:Key="TextColor">#FF0F3A2C</Color>
    <Color x:Key="MutedTextColor">#FF7FAF99</Color>

    <!-- Brushes -->
    <SolidColorBrush x:Key="WindowBackgroundBrush" Color="{StaticResource WindowBackgroundColor}" />
    <SolidColorBrush x:Key="WindowForegroundBrush" Color="{StaticResource WindowForegroundColor}" />

    <SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource PrimaryColor}" />
    <SolidColorBrush x:Key="PrimaryLightBrush" Color="{StaticResource PrimaryLightColor}" />
    <SolidColorBrush x:Key="PrimaryDarkBrush" Color="{StaticResource PrimaryDarkColor}" />
    <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}" />
    <SolidColorBrush x:Key="AccentDarkBrush" Color="{StaticResource AccentDarkColor}" />

    <SolidColorBrush x:Key="BackgroundBrush" Color="{StaticResource BackgroundColor}" />
    <SolidColorBrush x:Key="SurfaceBrush" Color="{StaticResource SurfaceColor}" />
    <SolidColorBrush x:Key="ElevatedSurfaceBrush" Color="{StaticResource ElevatedSurfaceColor}" />

    <SolidColorBrush x:Key="TextBrush" Color="{StaticResource TextColor}" />
    <SolidColorBrush x:Key="MutedTextBrush" Color="{StaticResource MutedTextColor}" />

    <!-- Themed gradient for larger areas -->
    <LinearGradientBrush x:Key="WindowBackgroundGradient" StartPoint="0,0" EndPoint="0,1">
        <GradientStop Color="{StaticResource PrimaryLightColor}" Offset="0" />
        <GradientStop Color="{StaticResource WindowBackgroundColor}" Offset="1" />
    </LinearGradientBrush>

    <!-- Ensure common controls are readable under Spring -->
    <Style TargetType="RadioButton">
        <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
    </Style>

    <Style TargetType="CheckBox">
        <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
    </Style>

</ResourceDictionary>