<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Glacier theme: icy blues, cool teals and crisp dark text -->
    <!-- Core window colors -->
    <Color x:Key="WindowBackgroundColor">#FFF4FBFF</Color>   <!-- very pale ice -->
    <Color x:Key="WindowForegroundColor">#FF083042</Color>   <!-- deep glacier teal -->

    <!-- Primary / accents -->
    <Color x:Key="PrimaryColor">#FF4FC3F7</Color>            <!-- sky/ice blue -->
    <Color x:Key="PrimaryLightColor">#FFBEEFFE</Color>       <!-- pale highlight -->
    <Color x:Key="PrimaryDarkColor">#FF1E88E5</Color>        <!-- strong blue -->
    <Color x:Key="AccentColor">#FF00BFA5</Color>             <!-- cool teal accent -->
    <Color x:Key="AccentDarkColor">#FF00796B</Color>

    <!-- Surface / neutrals -->
    <Color x:Key="BackgroundColor">#FFF4FBFF</Color>
    <Color x:Key="SurfaceColor">#FFF0F7FB</Color>
    <Color x:Key="ElevatedSurfaceColor">#FFE9F4F9</Color>

    <!-- Text palettes -->
    <Color x:Key="TextColor">#FF083042</Color>
    <Color x:Key="MutedTextColor">#FF6C8A97</Color>

    <!-- Brushes -->
    <SolidColorBrush x:Key="WindowBackgroundBrush" Color="{StaticResource WindowBackgroundColor}" />
    <SolidColorBrush x:Key="WindowForegroundBrush" Color="{StaticResource WindowForegroundColor}" />

    <SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource PrimaryColor}" />
    <SolidColorBrush x:Key="PrimaryLightBrush" Color="{StaticResource PrimaryLightColor}" />
    <SolidColorBrush x:Key="PrimaryDarkBrush" Color="{StaticResource PrimaryDarkColor}" />
    <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}" />
    <SolidColorBrush x:Key="AccentDarkBrush" Color="{StaticResource AccentDarkColor}" />

    <SolidColorBrush x:Key="BackgroundBrush" Color="{StaticResource BackgroundColor}" />
    <SolidColorBrush x:Key="SurfaceBrush" Color="{StaticResource SurfaceColor}" />
    <SolidColorBrush x:Key="ElevatedSurfaceBrush" Color="{StaticResource ElevatedSurfaceColor}" />

    <SolidColorBrush x:Key="TextBrush" Color="{StaticResource TextColor}" />
    <SolidColorBrush x:Key="MutedTextBrush" Color="{StaticResource MutedTextColor}" />

    <!-- Themed gradient for larger areas -->
    <LinearGradientBrush x:Key="WindowBackgroundGradient" StartPoint="0,0" EndPoint="0,1">
        <GradientStop Color="{StaticResource WindowBackgroundColor}" Offset="0" />
        <GradientStop Color="#FFE6F6FF" Offset="0.6" />
        <GradientStop Color="#FFDCEFF9" Offset="1" />
    </LinearGradientBrush>

    <!-- Ensure common controls are readable under Glacier -->
    <Style TargetType="RadioButton">
        <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
    </Style>

    <Style TargetType="CheckBox">
        <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
    </Style>

</ResourceDictionary>